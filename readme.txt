graf.c ->codigo para graficar lo obtenido con C++ 
 
codigo principal-> codigo usado para el proyecto
                    
               leer los comentarios si se quiere cambiar 
               algunos parametros

algoritmo genetico explicado-> bonus de un TSP euclidiano resuelto en python

graf.c->codigo en C++ para graficar el costo por iteracion con ROOT.
La funcion graf.c recibe un parametro que es la cantidad de iteraciones (y/o lineas) que guarda el txt, grafica el costo en cada iteración del algoritmo (la iteración es la linea en que fue escrita).

datos.txt-> Datos de prueba para graficar en caso que se desee probar el codigo en ROOT de graf.c.
